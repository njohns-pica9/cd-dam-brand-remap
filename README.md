cd-dam-brand-remap
==================

Remap brands


## Config
```json
{
    "db_connection": "postgres://username:password@hostname/dbname?sslmode=disable",
    "mapping": {
        "key": "value"
    }
}
```
